# .env.example をコピーして .env を作成してください
# cp .env.example .env

# 管理画面のパスワード（必ず変更してください）
ADMIN_PASSWORD=your_password_here

# データベースファイルのパス（デフォルト: results.db）
DB_PATH=results.db

# サーバーのポート番号（デフォルト: 5000）
PORT=5000

# デバッグモード（本番環境では false にしてください）
DEBUG=false
